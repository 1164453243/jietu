# 截图软件功能设计文档与 UI 设计方案

> 基于 QQ 截图的核心体验，扩展为一款面向办公、沟通、学习、设计协作场景的独立截图软件。

---

## 1. 产品概述

### 1.1 产品名称

暂定名：**SnapX 截图助手**

### 1.2 产品定位

SnapX 是一款面向办公、沟通、学习、设计协作场景的轻量级截图工具。

产品以 **快速截图、即时标注、智能识别、便捷分享** 为核心，参考 QQ 截图的低门槛交互，扩展长截图、OCR、贴图、录屏、取色、测距等高级能力。

### 1.3 产品目标

降低用户截图、标注、复制、分享、保存的操作成本，让截图成为一种即时表达工具，而不是单纯的图片截取工具。

### 1.4 目标用户

主要用户包括：

- 办公人员
- 客服人员
- 产品经理
- 设计师
- 开发者
- 教师
- 学生
  

- 自媒体创作者

核心共性是：他们经常需要截取屏幕内容，并在截图上进行说明、强调、隐藏敏感信息或快速分享。

---

## 2. 核心使用场景

### 2.1 办公沟通截图

用户在聊天、会议或协作文档中发现问题，需要快速截图并添加箭头、框选、文字说明，然后复制到聊天窗口。

核心流程：

1. 按快捷键唤起截图。
2. 拖拽选择区域。
3. 添加箭头、矩形框、文字。
4. 点击复制或按 Enter。
5. 粘贴到微信、QQ、飞书、钉钉、Slack 等工具。

### 2.2 产品 Bug 反馈

测试人员发现页面异常，需要截图并标注问题区域，同时隐藏用户手机号、姓名等敏感信息。

核心流程：

1. 截取页面。
2. 使用马赛克遮盖隐私。
3. 用序号标注多个问题点。
4. 保存本地或复制到缺陷管理平台。

### 2.3 长页面内容保存

用户需要保存网页、聊天记录、表格或文档中的长内容。

核心流程：

1. 选择长截图。
2. 框选滚动区域。
3. 自动滚动并拼接。
4. 预览长图。
5. 保存或复制。

### 2.4 图片文字提取

用户看到图片、网页、PDF 或视频画面中的文字，需要快速复制文本。

核心流程：

1. 框选文字区域。
2. 点击 OCR / 文字识别。
3. 系统展示识别结果。
4. 用户复制、翻译或保存文本。

### 2.5 临时参考资料钉图

用户需要把截图内容固定在桌面上，用于对照表格、写代码、看资料或比对设计稿。

核心流程：

1. 截图。
2. 点击“钉图”。
3. 截图悬浮在桌面最上层。
4. 支持缩放、透明度调整、关闭。

---

## 3. 功能架构

### 3.1 一级功能模块

| 模块 | 功能说明 |
|---|---|
| 快速截图 | 区域截图、窗口截图、全屏截图、延时截图 |
| 截图编辑 | 画笔、矩形、圆形、箭头、直线、文字、序号、马赛克、模糊 |
| 智能能力 | OCR 文字识别、图片翻译、二维码识别、图片识别 |
| 长截图 | 网页长截图、聊天长截图、滚动区域截图 |
| 贴图钉图 | 截图钉在桌面、置顶显示、缩放、透明度 |
| 录屏 | 区域录屏、窗口录屏、全屏录屏、GIF 录制 |
| 辅助工具 | 取色、测距、放大镜、历史记录 |
| 输出分享 | 复制、保存、拖拽分享、云端链接、导出格式 |
| 设置中心 | 快捷键、保存路径、图片格式、水印、主题、账号 |

---

## 4. 详细功能设计

## 4.1 截图启动

### 功能描述

用户可通过快捷键、托盘菜单、右键菜单或主界面按钮启动截图。

### 功能入口

| 入口 | 说明 |
|---|---|
| 快捷键 | 默认 Alt + A，可自定义 |
| 托盘图标 | 单击打开截图，右键打开菜单 |
| 主界面按钮 | 点击“开始截图” |
| 右键菜单 | 对图片文件右键，可选择“用 SnapX 编辑” |
| 悬浮球 | 可选，适合高频用户 |

### 交互规则

启动截图后，屏幕变暗，鼠标变为十字准星。移动鼠标时，系统自动识别当前窗口或控件区域，并显示高亮边框。用户可点击选中窗口，也可拖拽自定义区域。

### 截图模式

| 模式 | 说明 |
|---|---|
| 区域截图 | 用户自由拖拽选择截图区域 |
| 窗口截图 | 鼠标悬停自动识别窗口 |
| 全屏截图 | 一键截取当前屏幕 |
| 多屏截图 | 支持选择指定显示器 |
| 延时截图 | 3 秒、5 秒、10 秒后截图 |
| 固定尺寸截图 | 输入宽高，例如 1920 × 1080 |
| 上次区域截图 | 复用上一次截图范围 |

---

## 4.2 区域选择

### 交互状态

| 状态 | 说明 |
|---|---|
| 未选择 | 屏幕蒙层显示，等待用户拖拽 |
| 选择中 | 实时显示选区尺寸 |
| 已选择 | 显示编辑工具栏、尺寸信息、拖拽控制点 |
| 编辑中 | 用户可添加标注、文字、马赛克 |
| 输出中 | 保存、复制、分享或钉图 |

### 选区操作

1. 拖拽创建截图区域。
2. 拖动边缘调整大小。
3. 拖动区域内部移动选区。
4. 按 Esc 取消截图。
5. 按 Enter 复制截图。
6. 按 Ctrl + S 保存截图。
7. 按 Ctrl + C 复制截图。
8. 双击选区快速完成截图。

### 尺寸提示

选区右下角显示当前宽高，例如：

```text
1280 × 720
```

如果启用设计师模式，可额外显示坐标信息：

```text
X: 320  Y: 180  W: 1280  H: 720
```

---

## 4.3 截图编辑工具

### 工具栏功能

| 工具 | 说明 |
|---|---|
| 矩形 | 框选重点区域 |
| 圆形/椭圆 | 圈出重点区域 |
| 箭头 | 指向问题或说明位置 |
| 直线 | 用于连接或划线 |
| 画笔 | 自由涂鸦 |
| 荧光笔 | 半透明高亮内容 |
| 文字 | 添加说明文字 |
| 序号 | 自动生成 1、2、3 步骤标注 |
| 马赛克 | 隐藏敏感信息 |
| 模糊 | 隐藏隐私内容 |
| 橡皮擦 | 删除局部标注 |
| 撤销 | 撤销上一步操作 |
| 重做 | 恢复上一步撤销 |
| 裁剪 | 二次裁剪截图 |
| 旋转 | 左右旋转 90° |
| 取色 | 获取屏幕颜色 |
| 测距 | 测量像素距离 |

### 标注属性

每个标注工具支持以下属性：

| 属性 | 说明 |
|---|---|
| 颜色 | 默认红色，可选常用色 |
| 粗细 | 1px、2px、4px、8px |
| 透明度 | 20%～100% |
| 字号 | 12、14、16、20、24、32 |
| 字体 | 默认系统字体 |
| 箭头样式 | 单箭头、双箭头、无箭头 |
| 填充 | 无填充、半透明填充、实心填充 |

### 默认工具顺序

截图工具不是专业修图软件，编辑体验应轻量、快速、少干扰。工具栏默认展示高频工具，低频工具收纳到“更多”。

推荐默认工具顺序：

```text
矩形 / 箭头 / 画笔 / 文字 / 序号 / 马赛克 / OCR / 钉图 / 保存 / 复制 / 完成
```

---

## 4.4 马赛克与隐私保护

### 功能描述

用户可对截图中的隐私内容进行遮挡，例如手机号、邮箱、身份证号、头像、聊天记录、订单号等。

### 遮挡方式

| 类型 | 说明 |
|---|---|
| 马赛克 | 像素块遮挡 |
| 模糊 | 高斯模糊遮挡 |
| 涂黑 | 黑色实心遮挡 |
| 智能隐私识别 | 自动检测手机号、邮箱、身份证、银行卡号 |

### 智能隐私识别

开启后，系统在截图完成后自动扫描图像文本，识别可能的隐私字段，并以虚线框提示用户是否一键打码。

识别内容包括：

| 类型 | 示例 |
|---|---|
| 手机号 | 138****8888 |
| 邮箱 | user@example.com |
| 身份证号 | 18 位身份证号码 |
| 银行卡号 | 长数字串 |
| 地址 | 省市区街道信息 |
| 头像 | 人脸区域 |
| 昵称 | 聊天窗口用户名 |

### 安全规则

1. 马赛克操作应直接作用于最终图像像素，避免导出后被还原。
2. 保存历史记录时，默认保存已打码版本。
3. 用户开启“隐私模式”后，截图历史不保存原图。
4. OCR 默认本地识别，云端识别需用户确认。

---

## 4.5 OCR 文字识别

### 功能描述

用户可对截图区域或已有图片进行文字识别，提取可复制文本。

### 入口

| 入口 | 说明 |
|---|---|
| 截图工具栏 OCR 按钮 | 对当前截图区域识别 |
| 独立 OCR 快捷键 | 直接框选区域识别文字 |
| 图片右键菜单 | 对本地图片识别 |
| 历史记录 | 对历史截图再次识别 |

### 识别结果面板

识别完成后，在截图区域右侧弹出结果面板：

```text
识别结果
--------------------------------
这里显示从图片中识别出来的文字内容。

[复制全部] [翻译] [导出 TXT] [重新识别]
```

### 支持语言

| 语言 | 优先级 |
|---|---|
| 简体中文 | P0 |
| 英文 | P0 |
| 繁体中文 | P1 |
| 日文 | P1 |
| 韩文 | P1 |
| 多语言混排 | P1 |

### OCR 结果操作

| 操作 | 说明 |
|---|---|
| 复制全文 | 复制识别文本 |
| 复制选中 | 选择部分文字复制 |
| 翻译 | 调用翻译服务 |
| 导出 TXT | 保存文本文件 |
| 导出 Markdown | 保留段落结构 |
| 表格识别 | 识别表格并导出 CSV / Excel |
| 重新识别 | 调整区域后再次识别 |

---

## 4.6 图片翻译

### 功能描述

用户框选截图中的外语内容后，系统识别文字并翻译，适用于网页、软件界面、论文、报错信息、游戏内容等。

### 交互流程

1. 用户截图。
2. 点击“翻译”。
3. 系统执行 OCR。
4. 显示原文和译文。
5. 用户可复制译文或将译文覆盖到图片上。

### 翻译模式

| 模式 | 说明 |
|---|---|
| 文字面板翻译 | 右侧展示原文和译文 |
| 图片覆盖翻译 | 在原图位置覆盖译文 |
| 双语对照 | 原文与译文上下排列 |
| 悬浮翻译 | 鼠标悬停查看译文 |

---

## 4.7 长截图

### 功能描述

用户可截取网页、聊天窗口、文档、表格等需要滚动才能完整展示的内容。

### 长截图模式

| 模式 | 说明 |
|---|---|
| 自动滚动截图 | 系统自动滚动并拼接 |
| 手动滚动截图 | 用户滚动，系统捕捉变化 |
| 网页长截图 | 针对浏览器网页优化 |
| 聊天长截图 | 针对聊天记录优化 |
| 横向长截图 | 用于横向表格或看板 |
| 窗口长截图 | 选择窗口后自动识别滚动区域 |

### 交互流程

1. 用户启动截图。
2. 选择“长截图”。
3. 框选滚动区域。
4. 点击“开始滚动”。
5. 系统自动捕捉并拼接。
6. 右侧实时显示长图预览。
7. 用户点击“完成”。
8. 进入编辑或输出流程。

### 异常处理

| 异常 | 处理方式 |
|---|---|
| 滚动失败 | 提示用户手动滚动 |
| 拼接错位 | 提供“重新拼接”按钮 |
| 内容重复 | 自动去重，允许手动裁剪 |
| 页面加载慢 | 暂停等待加载 |
| 视频/动图区域变化 | 提示该区域可能影响拼接 |

---

## 4.8 钉图 / 贴图

### 功能描述

将截图变成桌面悬浮图片，始终置顶，方便用户临时查看、对照或参考。

### 钉图操作

| 操作 | 说明 |
|---|---|
| 拖动 | 移动钉图位置 |
| 滚轮 | 缩放钉图 |
| 双击 | 放大 / 还原 |
| 右键 | 打开菜单 |
| Esc | 取消当前选中 |
| 关闭按钮 | 关闭钉图 |

### 钉图菜单

```text
复制图片
保存图片
识别文字
翻译图片
调整透明度
锁定位置
置顶 / 取消置顶
关闭
```

### 钉图属性

| 属性 | 说明 |
|---|---|
| 透明度 | 20%～100% |
| 缩放比例 | 25%～400% |
| 边框 | 默认细阴影边框 |
| 置顶 | 默认开启 |
| 锁定 | 防止误拖动 |
| 分组 | 多张钉图可归组 |

---

## 4.9 录屏与 GIF

### 功能描述

用户可录制屏幕区域、窗口或全屏，用于演示操作流程、反馈 Bug、制作教程。

### 录制模式

| 模式 | 说明 |
|---|---|
| 区域录制 | 用户框选区域 |
| 窗口录制 | 选择指定窗口 |
| 全屏录制 | 录制整个屏幕 |
| GIF 录制 | 输出轻量动图 |
| 摄像头叠加 | 可选显示摄像头画面 |
| 麦克风录制 | 录制讲解声音 |
| 系统声音 | 录制电脑内部声音 |

### 录屏控制条

```text
● 00:01:23     暂停     停止     标注     麦克风     设置
```

### 输出格式

| 格式 | 用途 |
|---|---|
| MP4 | 默认视频格式 |
| GIF | 短动图 |
| WebM | Web 场景 |
| PNG 序列 | 高级用户 |

---

## 4.10 取色与测距

### 取色

用于设计、前端、视觉检查场景。

显示内容：

```text
HEX: #1677FF
RGB: 22, 119, 255
HSL: 212, 100%, 54%
```

操作：

1. 启动取色器。
2. 鼠标移动显示放大镜。
3. 点击复制颜色值。
4. 可切换 HEX / RGB / HSL / CMYK。

### 测距

用于测量两个元素之间的像素距离。

| 功能 | 说明 |
|---|---|
| 水平测距 | 测量左右间距 |
| 垂直测距 | 测量上下间距 |
| 元素尺寸 | 测量宽高 |
| 边缘吸附 | 自动贴合元素边缘 |
| 标注导出 | 将测距结果写入截图 |

---

## 4.11 历史记录

### 功能描述

保存用户最近截图，方便再次复制、编辑、识别或查找。

### 历史记录字段

| 字段 | 说明 |
|---|---|
| 缩略图 | 截图预览 |
| 创建时间 | 截图时间 |
| 来源 | 区域截图、长截图、OCR、录屏 |
| 尺寸 | 图片宽高 |
| 文件大小 | 图片大小 |
| 标签 | 用户自定义标签 |
| 保存路径 | 本地文件路径 |

### 隐私策略

1. 默认保存最近 30 条。
2. 用户可关闭历史记录。
3. 隐私模式下不保存截图。
4. 涉及 OCR 的文本结果可选择是否保存。
5. 支持一键清空历史。

---

## 4.12 输出与分享

### 输出方式

| 操作 | 说明 |
|---|---|
| 复制到剪贴板 | 默认完成动作 |
| 保存本地 | 保存为 PNG / JPG / WebP |
| 另存为 | 选择路径和格式 |
| 拖拽分享 | 拖动截图到聊天窗口 |
| 云端分享 | 生成临时链接 |
| 打开编辑器 | 进入完整编辑界面 |
| 打印 | 直接打印截图 |

### 默认快捷操作

| 快捷键 | 动作 |
|---|---|
| Enter | 完成并复制 |
| Ctrl + C | 复制 |
| Ctrl + S | 保存 |
| Ctrl + Z | 撤销 |
| Ctrl + Shift + Z | 重做 |
| Esc | 取消 |
| Space | 临时移动选区 |
| Tab | 切换工具 |
| 1～9 | 快速选择工具 |

---

# 5. UI 设计方案

## 5.1 整体设计原则

### 轻量

截图过程中的 UI 不能喧宾夺主。界面应尽量贴近 QQ 截图的即时工具条体验，避免复杂面板遮挡用户内容。

### 即时

用户完成截图后，应能在 1 秒内看到工具栏，并在最少操作下复制或保存。

### 聚焦

选区之外使用半透明暗色蒙层，选区内保持清晰。工具栏靠近选区，但不能遮挡主要内容。

### 可预期

所有工具使用常见图标：箭头、矩形、文字 T、马赛克、保存、复制、完成。不要发明过于抽象的图标。

---

## 5.2 截图态 UI

### 页面结构

```text
┌──────────────────────────────────────────────┐
│                  暗色蒙层区域                  │
│                                              │
│      ┌──────────────────────────────┐        │
│      │                              │        │
│      │          截图选区             │        │
│      │                              │        │
│      └──────────────────────────────┘        │
│              1280 × 720                      │
│        [工具栏：矩形 箭头 文字 马赛克 OCR...]  │
└──────────────────────────────────────────────┘
```

### 视觉样式

| 元素 | 样式 |
|---|---|
| 蒙层 | 黑色，透明度 45% |
| 选区边框 | 蓝色 1px 实线 |
| 控制点 | 8 个白色小圆点 |
| 尺寸提示 | 黑底白字，圆角 4px |
| 工具栏 | 深色半透明背景，圆角 8px |
| 工具图标 | 线性图标，白色 |
| 激活工具 | 蓝色高亮背景 |

---

## 5.3 工具栏 UI

### 默认工具栏

```text
┌──────────────────────────────────────────────────────┐
│ □  ○  ↗  ─  ✎  T  ①  ▦  👁  OCR  📌  ↶  ↷  💾  ✓ │
└──────────────────────────────────────────────────────┘
```

### 工具栏分组

| 分组 | 工具 |
|---|---|
| 标注工具 | 矩形、圆形、箭头、直线、画笔、文字、序号 |
| 隐私工具 | 马赛克、模糊、涂黑 |
| 智能工具 | OCR、翻译、二维码识别 |
| 输出工具 | 钉图、复制、保存、完成 |
| 历史操作 | 撤销、重做、清空 |

### 工具属性浮层

当用户选择工具后，在工具栏上方弹出小面板：

```text
颜色：● ● ● ● ●
粗细：1 2 4 8
透明度：━━━●────
```

### 设计细节

1. 工具栏跟随选区底部显示。
2. 如果选区靠近屏幕底部，工具栏自动显示在选区顶部。
3. 如果空间不足，工具栏折叠为两行或显示“更多”。
4. 鼠标离开 2 秒后，工具栏可轻微降低透明度。
5. 鼠标悬停工具图标显示名称和快捷键。

---

## 5.4 OCR 结果面板 UI

### 布局

```text
┌────────────────────────────┐
│ 文字识别                    │
│ 简体中文 / 自动检测          │
├────────────────────────────┤
│ 这里是识别出来的文本内容……   │
│                            │
│                            │
├────────────────────────────┤
│ [复制全部] [翻译] [导出]     │
└────────────────────────────┘
```

### 面板规则

| 规则 | 说明 |
|---|---|
| 出现位置 | 默认在选区右侧 |
| 宽度 | 320px |
| 高度 | 跟随选区高度，最大 70% 屏高 |
| 可拖动 | 支持 |
| 可折叠 | 支持 |
| 可复制 | 支持全文复制和选中复制 |

---

## 5.5 长截图 UI

### 进入长截图后

```text
┌──────────────────────────────┐
│ 选择滚动区域                  │
│                              │
│ ┌──────────────────────────┐ │
│ │                          │ │
│ │       滚动截图区域        │ │
│ │                          │ │
│ └──────────────────────────┘ │
│                              │
│ [开始滚动] [手动滚动] [取消]  │
└──────────────────────────────┘
```

### 截取中状态

```text
正在生成长截图……
已截取 5 屏
[暂停] [完成] [取消]
```

### 预览状态

```text
┌─────────────┬────────────────────┐
│ 长图预览     │ 编辑区域             │
│ 缩略导航     │                    │
│             │                    │
│             │                    │
└─────────────┴────────────────────┘
```

### 关键交互

1. 左侧显示长图缩略导航。
2. 右侧显示当前编辑区。
3. 重复区域自动标记。
4. 用户可手动删除重复片段。
5. 支持二次裁剪顶部和底部。

---

## 5.6 钉图 UI

### 钉图默认样式

```text
┌─────────────────────┐
│                     │
│       图片内容       │
│                     │
└─────────────────────┘
```

边缘使用轻微阴影，不显示明显工具栏。鼠标悬停时显示操作按钮：

```text
[复制] [OCR] [透明度] [锁定] [关闭]
```

### 右键菜单

```text
复制
保存
识别文字
翻译
调整透明度
缩放 100%
锁定位置
置顶
关闭
```

---

## 5.7 主界面 UI

虽然截图工具以快捷键为核心，但仍需要一个主界面承载历史记录、设置、账号和帮助。

### 首页布局

```text
┌──────────────────────────────────────────────┐
│ SnapX 截图助手                   设置  帮助   │
├──────────────────────────────────────────────┤
│ [开始截图] [长截图] [OCR] [录屏] [取色]       │
├──────────────────────────────────────────────┤
│ 最近截图                                      │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐          │
│ │缩略图│ │缩略图│ │缩略图│ │缩略图│          │
│ └──────┘ └──────┘ └──────┘ └──────┘          │
└──────────────────────────────────────────────┘
```

### 首页模块

| 模块 | 说明 |
|---|---|
| 快捷操作区 | 截图、长截图、OCR、录屏、取色 |
| 最近截图 | 最近 30 条截图 |
| 搜索栏 | 按 OCR 内容、时间、标签搜索 |
| 设置入口 | 快捷键、保存路径、主题 |
| 帮助入口 | 快捷键说明、反馈、版本信息 |

---

## 5.8 设置界面 UI

### 设置分组

```text
通用设置
- 开机启动
- 托盘常驻
- 截图后默认动作

快捷键
- 区域截图
- 长截图
- OCR
- 录屏
- 取色

保存设置
- 默认保存路径
- 文件命名规则
- 默认图片格式
- 图片质量

隐私设置
- 是否保存历史
- 是否保存 OCR 结果
- 隐私模式
- 自动识别敏感信息

外观设置
- 浅色 / 深色 / 跟随系统
- 工具栏大小
- 主题色
```

### 文件命名规则

默认：

```text
Screenshot_yyyyMMdd_HHmmss.png
```

可选变量：

```text
{date}_{time}_{width}x{height}.{format}
```

---

# 6. 关键页面原型草图

## 6.1 截图编辑态

```text
屏幕蒙层
┌──────────────────────────────────────────────────────┐
│                                                      │
│        ┌──────────────────────────────────┐          │
│        │                                  │          │
│        │        当前截图选区               │          │
│        │                                  │          │
│        └──────────────────────────────────┘          │
│                         960 × 540                    │
│        ┌──────────────────────────────────┐          │
│        │ □ ○ ↗ ─ ✎ T ① ▦ OCR 📌 ↶ ↷ 💾 ✓ │          │
│        └──────────────────────────────────┘          │
│                                                      │
└──────────────────────────────────────────────────────┘
```

## 6.2 OCR 识别态

```text
┌─────────────────────────────┐   ┌──────────────────────┐
│                             │   │  文字识别              │
│        截图区域              │   │  自动检测：中文         │
│                             │   ├──────────────────────┤
│                             │   │  识别文本内容……         │
│                             │   │                      │
└─────────────────────────────┘   ├──────────────────────┤
                                  │ [复制] [翻译] [导出]  │
                                  └──────────────────────┘
```

## 6.3 长截图态

```text
┌──────────────┬──────────────────────────────────┐
│ 长图缩略导航  │                                  │
│              │          长截图预览区             │
│              │                                  │
│              │                                  │
├──────────────┴──────────────────────────────────┤
│ [重新截取] [裁剪] [标注] [复制] [保存] [完成]     │
└─────────────────────────────────────────────────┘
```

---

# 7. 状态与反馈设计

## 7.1 成功反馈

| 操作 | 反馈 |
|---|---|
| 复制成功 | Toast：已复制到剪贴板 |
| 保存成功 | Toast：已保存到 xxx 路径 |
| OCR 成功 | 展示识别结果 |
| 钉图成功 | 图片悬浮显示 |
| 录屏完成 | 展示视频预览 |

## 7.2 失败反馈

| 场景 | 提示 |
|---|---|
| 保存失败 | 保存失败，请检查路径权限 |
| OCR 失败 | 未识别到文字，请调整截图区域 |
| 长截图失败 | 自动滚动失败，可尝试手动滚动 |
| 录屏失败 | 录屏权限不足，请检查系统设置 |
| 快捷键冲突 | 当前快捷键已被占用，请重新设置 |

---

# 8. 权限设计

## 8.1 Windows

需要权限：

1. 屏幕捕获权限。
2. 文件读写权限。
3. 麦克风权限，录屏时可选。
4. 系统声音录制能力。
5. 开机启动权限，可选。

## 8.2 macOS

需要权限：

1. 屏幕录制权限。
2. 辅助功能权限，用于窗口识别和滚动截图。
3. 文件与文件夹访问权限。
4. 麦克风权限，录屏时可选。

---

# 9. 非功能需求

## 9.1 性能

| 指标 | 要求 |
|---|---|
| 截图启动速度 | 小于 300ms |
| 区域选择延迟 | 小于 16ms |
| 标注响应 | 实时无明显卡顿 |
| OCR 普通截图 | 3 秒内完成 |
| 长截图拼接 | 每屏拼接小于 1 秒 |
| 内存占用 | 常驻小于 150MB |
| 安装包大小 | 尽量控制在 100MB 内 |

## 9.2 稳定性

1. 截图过程中不得导致当前应用失焦异常。
2. 多屏场景下坐标计算准确。
3. 高 DPI 屏幕下选区尺寸准确。
4. 断网时基础截图、标注、保存仍可使用。
5. OCR、翻译等云能力失败不影响基础截图。

## 9.3 兼容性

| 平台 | 优先级 |
|---|---|
| Windows 10 / 11 | P0 |
| macOS 12+ | P1 |
| Linux | P2 |
| 浏览器插件 | P2 |

---

# 10. MVP 版本规划

## 10.1 V1.0：基础截图版

目标：实现类似 QQ 截图的核心体验。

包含功能：

1. 快捷键截图。
2. 区域截图。
3. 窗口识别。
4. 基础标注。
5. 马赛克。
6. 复制到剪贴板。
7. 保存本地。
8. 简单设置。

不包含：

1. 长截图。
2. OCR。
3. 录屏。
4. 云分享。
5. 历史记录高级搜索。

## 10.2 V1.5：智能增强版

新增：

1. OCR 文字识别。
2. 图片翻译。
3. 二维码识别。
4. 钉图。
5. 历史记录。
6. 自定义快捷键。

## 10.3 V2.0：专业效率版

新增：

1. 长截图。
2. 屏幕录制。
3. GIF 录制。
4. 取色器。
5. 测距工具。
6. 自动隐私打码。
7. 云端分享链接。
8. 团队协作能力。

---

# 11. 产品差异化建议

## 11.1 更强的隐私保护

截图软件天然容易截到敏感内容。可以加入“智能隐私打码”，自动识别手机号、邮箱、身份证号、头像、人名等信息。

## 11.2 更顺手的钉图体验

钉图是高频办公功能，尤其适合对照资料、抄代码、看设计稿。建议支持多图分组、透明度、锁定、缩放和 OCR。

## 11.3 更适合产品/研发协作

增加序号标注、步骤说明、测距、取色、导出 Markdown 等功能，让它不只是截图工具，而是“视觉沟通工具”。

## 11.4 本地优先

基础截图、标注、马赛克、保存、历史记录尽量本地完成。OCR 可优先本地识别，云端识别作为增强能力，避免用户担心隐私。

---

# 12. UI 视觉规范建议

## 12.1 色彩

| 用途 | 颜色 |
|---|---|
| 主色 | #1677FF |
| 成功 | #52C41A |
| 警告 | #FAAD14 |
| 错误 | #FF4D4F |
| 蒙层 | rgba(0, 0, 0, 0.45) |
| 工具栏背景 | rgba(24, 24, 24, 0.92) |
| 文字主色 | #FFFFFF |
| 辅助文字 | rgba(255, 255, 255, 0.65) |

## 12.2 圆角

| 元素 | 圆角 |
|---|---|
| 工具栏 | 8px |
| 按钮 | 6px |
| 浮层 | 8px |
| 颜色选择器 | 4px |
| 截图选区控制点 | 圆形 |

## 12.3 阴影

工具栏阴影：

```css
box-shadow: 0 8px 24px rgba(0, 0, 0, 0.24);
```

钉图阴影：

```css
box-shadow: 0 4px 16px rgba(0, 0, 0, 0.18);
```

## 12.4 动效

| 动效 | 说明 |
|---|---|
| 工具栏出现 | 100ms 淡入 + 上移 |
| 选区生成 | 边框实时跟随 |
| Toast | 160ms 淡入，2 秒后消失 |
| 钉图缩放 | 跟随鼠标滚轮即时缩放 |
| OCR 面板 | 从右侧轻微滑入 |

---

# 13. 研发实现建议

## 13.1 技术模块

| 模块 | 说明 |
|---|---|
| Screen Capture | 屏幕捕获 |
| Overlay Layer | 全屏蒙层和选区绘制 |
| Annotation Engine | 标注绘制引擎 |
| Image Processor | 图片裁剪、合成、压缩 |
| Clipboard Manager | 剪贴板读写 |
| OCR Engine | 本地 / 云端 OCR |
| Scroll Stitching | 长截图拼接 |
| Recorder | 屏幕录制 |
| History DB | 历史记录数据库 |
| Settings | 用户配置 |

## 13.2 数据结构示例

### 截图对象

```json
{
  "id": "shot_20260525_001",
  "type": "region",
  "width": 1280,
  "height": 720,
  "createdAt": "2026-05-25T10:30:00+08:00",
  "filePath": "/screenshots/shot_001.png",
  "annotations": [],
  "ocrText": "",
  "isPinned": false
}
```

### 标注对象

```json
{
  "id": "anno_001",
  "type": "arrow",
  "x": 120,
  "y": 80,
  "width": 200,
  "height": 80,
  "color": "#FF4D4F",
  "strokeWidth": 4,
  "opacity": 1
}
```

---

# 14. 验收标准

## 14.1 截图基础能力

| 验收项 | 标准 |
|---|---|
| 快捷键唤起 | 按快捷键后 300ms 内进入截图态 |
| 区域选择 | 可自由拖拽，尺寸显示准确 |
| 窗口识别 | 鼠标悬停窗口可高亮 |
| 标注 | 支持矩形、箭头、文字、马赛克 |
| 复制 | 完成后可粘贴到聊天工具 |
| 保存 | 可保存 PNG 到指定路径 |
| 取消 | Esc 可退出截图态 |

## 14.2 OCR 能力

| 验收项 | 标准 |
|---|---|
| 中文识别 | 常规清晰截图识别准确 |
| 英文识别 | 支持英文段落 |
| 复制结果 | 可一键复制全文 |
| 失败提示 | 无文字时给出明确提示 |

## 14.3 长截图能力

| 验收项 | 标准 |
|---|---|
| 自动滚动 | 支持网页自动滚动 |
| 拼接结果 | 无明显错位 |
| 预览 | 可查看完整长图 |
| 保存 | 可保存为 PNG / JPG |

---

# 15. 一句话产品总结

**SnapX 要做的是“像 QQ 截图一样顺手，但比 QQ 截图更完整、更智能、更适合办公协作”的截图工具。**
